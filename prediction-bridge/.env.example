# Runtime configuration file path (YAML)
PREDICTION_BRIDGE_CONFIG_PATH=./config/config.yaml

# Overrides for sensitive fields (take precedence over YAML).
# Use double underscores to address nested keys.
# MINIO__ACCESS_KEY=
# MINIO__SECRET_KEY=
# FEISHU__APP_ID=cli_xxx
# FEISHU__APP_SECRET=
# CALLBACK__SECRET=
